# Routify Dashboard — Mock Mode
# Run: npm run dev:mock
# All API calls and WebSocket connections are intercepted client-side.
# No backend services required.

VITE_MOCK=true
VITE_API_BASE_URL=http://localhost:8082

