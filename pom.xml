<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.5</version>
        <relativePath/>
    </parent>

    <groupId>io.routify</groupId>
    <artifactId>routify-parent</artifactId>
    <version>2.0.2-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Routify Parent</name>
    <description>Routify — Zero-downtime API Gateway Platform (Parent BOM)</description>

    <modules>
        <module>routify-common</module>
        <module>routify-identity-service</module>
        <module>routify-route-service</module>
        <module>routify-api-gateway</module>
        <module>routify-admin-api</module>
        <module>routify-audit-service</module>
        <module>routify-cert-vault</module>
        <module>routify-ai-service</module>
    </modules>

    <properties>
        <java.version>25</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Dependency versions -->
        <mapstruct.version>1.6.3</mapstruct.version>
        <commons-io.version>2.21.0</commons-io.version>
        <jjwt.version>0.13.0</jjwt.version>
        <spring-cloud.version>2025.1.1</spring-cloud.version>
        <resilience4j.version>2.2.0</resilience4j.version>
        <spring-ai.version>1.1.4</spring-ai.version>

        <!-- Testing -->
        <!-- Integration tests (*IT.java) disabled globally (Testcontainers + Docker Engine 29.x incompatibility).
             Re-enable with: mvn verify -DskipITs=false -->
        <skipITs>true</skipITs>
        <!-- testcontainers.version managed by Boot 4.0.5 (TC 2.0.4) — artifacts renamed with testcontainers- prefix -->
        <!-- Override docker-java for Docker Engine 29.x API compatibility (min API 1.40) -->
        <docker-java.version>3.7.1</docker-java.version>

        <!-- Override Spring Boot managed Lombok — pinned for JDK 25 compatibility -->
        <lombok.version>1.18.44</lombok.version>

    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Internal modules -->
            <dependency>
                <groupId>io.routify</groupId>
                <artifactId>routify-common</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring AI BOM — provider-agnostic ChatModel / ChatClient abstraction -->
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- MapStruct -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>

            <!-- Apache Commons (commons-lang3 managed by Boot 3.5.13) -->
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons-io.version}</version>
            </dependency>

            <!-- JWT -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>

            <!--
                Distributed Tracing: micrometer-tracing-bridge-otel and opentelemetry-exporter-otlp
                are version-managed by Spring Boot 3.5.13 BOM. Child modules declare them without version.
            -->

            <!-- ─── Testing ─────────────────────────────────────────────────────── -->

            <!-- Override docker-java to fix API version negotiation with Docker Engine 29.x -->
            <dependency>
                <groupId>com.github.docker-java</groupId>
                <artifactId>docker-java-api</artifactId>
                <version>${docker-java.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.docker-java</groupId>
                <artifactId>docker-java-transport-zerodep</artifactId>
                <version>${docker-java.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.docker-java</groupId>
                <artifactId>docker-java-transport</artifactId>
                <version>${docker-java.version}</version>
            </dependency>

            <!--
                Testcontainers: version-managed by Boot 4.0.5 BOM (TC 2.0.4).
                TC 2.x renamed artifacts with testcontainers- prefix (e.g. testcontainers-junit-jupiter).
                spring-boot-starter-test, spring-kafka-test, and spring-rabbit-test
                are also version-managed by the spring-boot-starter-parent BOM.
                Child modules declare TC modules with <scope>test</scope> only — no version needed.
            -->

        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <!-- ─── Unit tests ──────────────────────────────────────────────── -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                </plugin>

                <!-- ─── Integration tests (*IT.java) ────────────────────────────── -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <executions>
                        <execution>
                            <goals>
                                <goal>integration-test</goal>
                                <goal>verify</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <parameters>true</parameters>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                            <path>
                                <groupId>org.mapstruct</groupId>
                                <artifactId>mapstruct-processor</artifactId>
                                <version>${mapstruct.version}</version>
                            </path>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok-mapstruct-binding</artifactId>
                                <version>0.2.0</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <!--
            Quick verification — runs unit tests only, skips integration tests.
            Note: ITs are already disabled globally via <skipITs>true</skipITs>.
            This profile is kept for backwards compatibility (mvn verify -Pquick).
            Re-enable ITs with: mvn verify -DskipITs=false
        -->
        <profile>
            <id>quick</id>
            <properties>
                <skipITs>true</skipITs>
            </properties>
        </profile>
    </profiles>
</project>

