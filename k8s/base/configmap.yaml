# Phase 6.4: Shared ConfigMap for Routify services
apiVersion: v1
kind: ConfigMap
metadata:
  name: routify-config
  namespace: routify
data:
  kafka-bootstrap: "kafka:9092"
  rabbitmq-host: "rabbitmq"
  redis-host: "redis"
  db-host: "postgres"
  db-name: "routify"
  db-user: "routify"

