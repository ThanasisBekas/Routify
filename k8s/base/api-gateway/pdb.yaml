# Phase 6.4: PodDisruptionBudget — ensure at least 1 gateway replica is always available
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: routify-gateway-pdb
  namespace: routify
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: routify-gateway

